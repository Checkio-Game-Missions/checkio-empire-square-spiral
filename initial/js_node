"use strict";
function golf(first, second){
    return 1;
}
//
//var assert = require("assert");
//
//if (!global.is_checking) {
//    // These "asserts" using only for self-checking and not necessary for auto-testing
//    assert.equal(golf(1, 9), 2, "1st example");
//    assert.equal(golf(9, 1), 2, "2nd example");
//    assert.equal(golf(10, 25), 1, "3rd example");
//    assert.equal(golf(5, 9), 4, "4th example");
//}